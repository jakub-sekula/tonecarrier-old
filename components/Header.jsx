const Header = ({title}) => {
    return (
        <header className="w-screen h-24 bg-slate-200 text-center shrink-0 flex justify-center items-center">
            <h1>{title}</h1>
        </header>
    )
}

export default Header